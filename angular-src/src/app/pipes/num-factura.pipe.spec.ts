import { NumFacturaPipe } from './num-factura.pipe';

describe('NumFacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumFacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
